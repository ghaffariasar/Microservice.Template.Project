<div dir="rtl" align="right">

# لاگینگ (Serilog)

در تمام پروژه‌ها Serilog با `UseSerilog()` و `WriteTo.Console()` ثبت شده است. می‌توانید Sinkها (Elastic, Seq, File, ...) را از طریق `appsettings` اضافه کنید.

نمونه تنظیمات پایه در هر `Program.cs` موجود است.

</div>


